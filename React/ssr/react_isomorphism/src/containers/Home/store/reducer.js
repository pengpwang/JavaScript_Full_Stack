const defaultState = {
  name: 'Jack Ma',
  newsList: []
};

export default (state = defaultState, action) => {
  switch (action.type) {
    // case 1:
      
    //   break;
  
    default:
      return state;
  }
};